import { PageMainDirective } from './page-main.directive';

describe('PageMainDirective', () => {
  it('should create an instance', () => {
    const directive = new PageMainDirective();
    expect(directive).toBeTruthy();
  });
});
