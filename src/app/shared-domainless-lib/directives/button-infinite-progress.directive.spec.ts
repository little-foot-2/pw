import { ButtonInfiniteProgressDirective } from './button-infinite-progress.directive';

describe('ButtonInfiniteProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonInfiniteProgressDirective();
    expect(directive).toBeTruthy();
  });
});
