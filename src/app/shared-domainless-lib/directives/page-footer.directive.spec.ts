import { PageFooterDirective } from './page-footer.directive';

describe('PageFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new PageFooterDirective();
    expect(directive).toBeTruthy();
  });
});
