import { BpsIfDirective } from './bps-if.directive';

describe('BpsIfDirective', () => {
  it('should create an instance', () => {
    const directive = new BpsIfDirective();
    expect(directive).toBeTruthy();
  });
});
