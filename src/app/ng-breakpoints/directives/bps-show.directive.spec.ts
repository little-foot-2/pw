import { BpsShowDirective } from './bps-show.directive';

describe('BpsShowDirective', () => {
  it('should create an instance', () => {
    const directive = new BpsShowDirective();
    expect(directive).toBeTruthy();
  });
});
