import { BpsHideDirective } from './bps-hide.directive';

describe('BpsHideDirective', () => {
  it('should create an instance', () => {
    const directive = new BpsHideDirective();
    expect(directive).toBeTruthy();
  });
});
